<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Show Note</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body class="bg-gradient-to-r from-blue-50 to-purple-50 min-h-screen">

    <!-- Navbar -->
    <nav class="bg-gradient-to-r from-blue-600 to-purple-600 backdrop-blur-sm shadow-lg sticky top-0 z-50">
        <div class="max-w-7xl mx-auto px-6">
            <div class="flex justify-between items-center h-16">
                <!-- Logo and Title -->
                <div class="flex-shrink-0">
                    <a href="#" class="flex items-center space-x-3">
                        <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"/>
                        </svg>
                        <span class="font-bold text-2xl text-white hover:text-blue-200 transition-all duration-500">NoteKeeper Pro</span>
                    </a>
                </div>

                <!-- User Profile and Logout -->
                <div class="flex items-center space-x-8">
                    <div class="flex items-center space-x-4 bg-white/10 backdrop-blur-md px-6 py-2 rounded-full">
                        <div class="relative">
                            <img src="https://ui-avatars.com/api/?name=<%=user.username%>&background=random" alt="Profile" class="w-10 h-10 rounded-full border-2 border-white shadow-lg hover:scale-110 transition-transform duration-300">
                            <div class="absolute -bottom-1 -right-1 w-4 h-4 bg-green-400 rounded-full border-2 border-white"></div>
                        </div>
                        <div class="flex flex-col">
                            <span class="font-bold text-lg text-white"><%= user.username%></span>
                            <span class="text-sm text-blue-200">Online</span>
                        </div>
                    </div>

                    <a href="/logout" class="flex items-center space-x-2 bg-red-500 hover:bg-red-600 text-white px-6 py-2 rounded-full transition-all duration-300 hover:scale-105 shadow-lg group">
                        <span class="font-semibold">Logout</span>
                        <svg class="w-5 h-5 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"/>
                        </svg>
                    </a>
                </div>
            </div>
        </div>
    </nav>



    <!-- Show Note Section -->
    <div class="container mx-auto mt-12 px-4">
        <div class="bg-white p-8 rounded-xl shadow-2xl max-w-3xl mx-auto transform hover:scale-[1.02] transition-all duration-300">
            <!-- Note Header -->
            <div class="border-b pb-4 mb-6">
                <h2 class="text-4xl font-bold text-gray-800 mb-2"><%= notes.title %></h2>
                <div class="flex items-center text-gray-500">
                    <i class="fas fa-clock mr-2"></i>
                    <span>Created: <%= new Date().toLocaleDateString() %></span>
                </div>
            </div>
            
            <!-- Note Content -->
            <div class="prose max-w-none">
                <p class="text-gray-700 leading-relaxed text-lg whitespace-pre-wrap"><%= notes.content %></p>
            </div>

            <!-- Action Buttons -->
            <div class="mt-8 flex justify-end space-x-4">
                <a href="javascript:history.back()" 
                   class="px-6 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition duration-300 flex items-center">
                    <i class="fas fa-arrow-left mr-2"></i>
                    Back
                </a>
            </div>
        </div>
    </div>

    <!-- Footer -->
    <footer class="mt-12 text-center text-gray-600 pb-6">
        <p>© 2024 Notes App. All rights reserved.</p>
    </footer>

</body>
</html>
